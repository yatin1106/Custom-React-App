# Custom-React-App
A beginner-friendly project where I built a simplified version of React from scratch to understand how React works internally.


📌 What This Does
This project recreates the core ideas behind React:
Virtual DOM (JavaScript object representation of UI)
Rendering elements to the real DOM
Handling props dynamically
Supporting nested elements (component tree)
